name: Release

on:
  # Tag a commit to cut a release: `git tag vX.Y.Z && git push origin vX.Y.Z`
  # The tag (minus the leading `v`) becomes the packaged version.
  push:
    tags: ['v*']
  # Or build installable vsix files on demand, without publishing a release.
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    name: Verify before packaging
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run check-types
      - run: npm test

  package:
    name: Package ${{ matrix.target }}
    needs: verify
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Each target builds on its native runner, so `npm ci` installs the
          # correct @lancedb/lancedb-* native binary via optional dependencies.
          # Cross-installing native optional deps is fragile; this avoids it.
          - target: darwin-arm64
            os: macos-14
          - target: linux-x64
            os: ubuntu-latest
          - target: win32-x64
            os: windows-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      # tree-sitter grammars ship prebuilds for every platform; a targeted vsix
      # loads exactly one. Dropping the rest saves ~30 MB per artifact.
      - name: Prune foreign native prebuilds
        shell: bash
        run: node scripts/prune-native-prebuilds.mjs ${{ matrix.target }}

      # On a tag push the release version is the tag (v1.2.3 -> 1.2.3); that is
      # the source of truth for a cut release. workflow_dispatch builds have no
      # tag, so fall back to whatever package.json declares.
      - name: Resolve version
        id: version
        shell: bash
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            version="${GITHUB_REF#refs/tags/v}"
          else
            version="$(node -p "require('./package.json').version")"
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"

      # Sync package.json so vsce writes the cut-release version into the vsix
      # manifest, not the stale 0.1.0 checked into the repo.
      - name: Set package version
        shell: bash
        run: npm version "${{ steps.version.outputs.version }}" --no-git-tag-version --allow-same-version

      # Unsigned by design: no publisher certificate, no marketplace publish.
      # The .vsix installs via "Extensions: Install from VSIX..." or
      # `code --install-extension <file>`.
      - name: Package vsix
        shell: bash
        run: |
          npx vsce package \
            --target ${{ matrix.target }} \
            --out crucible-${{ steps.version.outputs.version }}-${{ matrix.target }}.vsix

      - name: Verify the vsix is loadable
        shell: bash
        run: node scripts/verify-vsix.mjs crucible-${{ steps.version.outputs.version }}-${{ matrix.target }}.vsix ${{ matrix.target }}

      - uses: actions/upload-artifact@v4
        with:
          name: crucible-${{ matrix.target }}
          path: '*.vsix'
          if-no-files-found: error
          retention-days: 30

  release:
    name: Attach vsix files to the GitHub release
    needs: package
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: List artifacts
        run: ls -lh artifacts

      - uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*.vsix
          generate_release_notes: true
          fail_on_unmatched_files: true
